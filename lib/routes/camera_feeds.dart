import 'package:flutter/widgets.dart';

class CameraFeedsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {

    return Container();
  }

  CameraFeedsPage();

  static CameraFeedsPage builder(BuildContext context) {
    return CameraFeedsPage();
  }
}
