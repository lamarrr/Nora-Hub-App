import 'package:flutter/widgets.dart';

class DoorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();}

  DoorsPage();

  static DoorsPage builder(BuildContext context) {
    return DoorsPage();
  }
}
