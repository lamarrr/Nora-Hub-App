import 'package:flutter/widgets.dart';

class DevicesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }

  DevicesPage();

  static DevicesPage builder(BuildContext context) {
    return DevicesPage();
  }
}
